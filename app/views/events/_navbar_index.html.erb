<div class="navbar-index">
  <% if current_user.nil? %>
    <a href="<%= new_user_session_path %>">
      <%= image_tag "avatar/default.png", class:"navbar-avatar" %>
    </a>
  <% else %>
    <a href="<%= logged_user_path %>">
      <%= cl_image_tag current_user.photo.key, height: 400, width: 400, crop: :crop, gravity: :face, id: "navbar-avatar" %>
    </a>
  <% end %>
  <div class="navbar-form-index">
    <%= form_tag events_path, method: :get do %>
      <%= text_field_tag :query,
        params[:query],
        class: "form-control",
        placeholder: "Find an event/T-Mate"  %>
      <% end %>
  </div>
    <div class="dropdown">
      <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Filters
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
        <%= link_to 'Date', events_path(dropdown: 'date') %>
        <%= link_to 'Distance', events_path(dropdown: 'distance') %>
        <%= link_to 'Teammate', events_path(dropdown: 'teammate') %>
        <div class="dropdown-divider"></div>
        <%= link_to 'Biking', events_path(dropdown: 'biking') %>
        <%= link_to 'Bowling', events_path(dropdown: 'bowling') %>
        <%= link_to 'Football', events_path(dropdown: 'football') %>
        <%= link_to 'Running', events_path(dropdown: 'running') %>
        <%= link_to 'Volleyball', events_path(dropdown: 'volleyball') %>
        <%= link_to 'Swimming', events_path(dropdown: 'swimming') %>
      </div>
    </div>
</div>
